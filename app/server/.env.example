# Server Configuration
PORT=3001

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=eu_law_platform_dev

# Optional: For pg library if you need to specify a different NODE_ENV
# NODE_ENV=development
